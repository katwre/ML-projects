seed: 42
num_workers: 4

paths:
  data_dir: data/chest_xray_224   # or: data/chest_xray_63
  out_dir: outputs/

train:
  batch_size: 64
  epochs: 100
  lr: 0.001
  weight_decay: 0.0001
  mixed_precision: false     # keep false for BCELoss+Sigmoid simplicity
  early_stop_patience: 25